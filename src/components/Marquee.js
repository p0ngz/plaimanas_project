function Marquee() {
  const section = document.createElement("div");
  section.className = "marquee-section";
  section.innerHTML= `Hello Marquee section`
  return section;
}

export default Marquee;
